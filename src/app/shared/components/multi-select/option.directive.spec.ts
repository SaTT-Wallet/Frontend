import { OptionDirective } from './option.directive';

describe('OptionDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionDirective();
    expect(directive).toBeTruthy();
  });
});
