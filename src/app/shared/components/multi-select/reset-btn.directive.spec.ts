import { ResetBtnDirective } from './reset-btn.directive';

describe('ResetBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetBtnDirective();
    expect(directive).toBeTruthy();
  });
});
