import { EarnedSattPipe } from './earned-satt.pipe';

describe('EarnedSattPipe', () => {
  it('create an instance', () => {
    const pipe = new EarnedSattPipe();
    expect(pipe).toBeTruthy();
  });
});
