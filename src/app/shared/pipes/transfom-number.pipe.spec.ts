import { TransfomNumberPipe } from './transfom-number.pipe';

describe('TransfomNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new TransfomNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
