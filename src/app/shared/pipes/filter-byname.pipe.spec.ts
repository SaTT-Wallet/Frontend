import { FilterBynamePipe } from './filter-byname.pipe';

describe('FilterBynamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBynamePipe();
    expect(pipe).toBeTruthy();
  });
});
