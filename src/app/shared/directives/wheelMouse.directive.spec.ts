import { WheelMouseDirective } from './wheelMouse.directive';

describe('WheelMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new WheelMouseDirective();
    expect(directive).toBeTruthy();
  });
});
