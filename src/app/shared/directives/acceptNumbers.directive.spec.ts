import { AcceptNumbersDirective } from './acceptNumbers.directive';

describe('AcceptNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = AcceptNumbersDirective;
    expect(directive).toBeTruthy();
  });
});
