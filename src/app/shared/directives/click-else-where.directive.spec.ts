import { ClickElseWhereDirective } from './click-else-where.directive';

describe('ClickElseWhereDirective', () => {
  it('should create an instance', () => {
    const directive =  ClickElseWhereDirective;
    expect(directive).toBeTruthy();
  });
});
